for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(BB_loop[2:10001] * diff(BB_loop))
}
mean(mean_arr)
median(mean_arr)
mean_arr <- vector('numeric')
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(BB_loop[2:10001] * diff(BB_loop) * delta_t)
}
mean(mean_arr)
median(mean_arr)
mean_arr <- vector('numeric')
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(BB_loop[2:10001] * diff(BB_loop) * delta_t)
}
round(mean(mean_arr), 5)
round(median(mean_arr), 5)
mean_arr <- vector('numeric')
for (i in 1:10000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop) * delta_t)
}
round(mean(mean_arr), 5)
round(median(mean_arr), 5)
mean_arr <- vector('numeric')
for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
# normal Bt dBt integration
mean_arr[i] <- sum(BB_loop[2:10001] * diff(BB_loop) * delta_t)
}
round(mean(mean_arr), 5)
round(median(mean_arr), 5)
mean_arr <- vector('numeric')
for (i in 1:1000) {
BB_loop <- cumsum(rnorm(length(t), 0, sqrt(delta_t)))
mean_arr[i] <- sum(exp(BB_loop[2:10001]) * diff(BB_loop) * delta_t)
}
round(mean(mean_arr), 5)
round(median(mean_arr), 5)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
X <- mu * delta + sigma * B
X_t <- cumsum(X)
plot(t, X_t, type = 'l')
mean(X_t)
var(X_t)
?rnorm
mean(B)
var(B)
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) * diff(B1)
diff(B).T * diff(B1)
transpose(diff(B)) * diff(B1)
?trans
transpose(diff(B)) %*% diff(B1)
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.0001
sigma <- 1
t <- seq(0, 1, delta)
?rnorm
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
?t
t(diff(B)) %*% diff(B1)
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.00001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
diff(B)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 10, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 1, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
plot(t, B2, type = 'l')
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
mean(B2)
mean(B)
mean(B1)
var(B2)
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
mean(B2)
var(B2)
B2 <- rnorm(length(t), 0, sqrt(t)) # delta is varianz, but needs to be standard deviation
X2 <- cumsum(B2)
plot(t, X2, type = 'l')
mean(B2)
var(B2)
mean(B)
var(B)
X <- mu * delta + sigma * B
diff(B) %*% diff(B1)
mu <- 0
delta <- 0.001
sigma <- 1
t <- seq(0, 100, delta)
B <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
B1 <- rnorm(length(t), 0, sqrt(delta)) # delta is varianz, but needs to be standard deviation
diff(B) %*% diff(B1)
B %*% B1
mean(B)
var(B)
var(diff(B))
X <- mu * delta + sigma * B
X_t <- cumsum(X)
plot(t, X_t, type = 'l')
source("C:/Users/pat_h/OneDrive/p-spohr-repos/R-Resources/Brownian Motion/Brownian Motion.R")
